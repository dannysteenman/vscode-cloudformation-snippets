{
	"Fn::Base64": {
		"prefix": "base64",
		"body": "!Base64 valueToEncode",
		"description": "The intrinsic function Fn::Base64 returns the Base64 representation of the input string. This function is typically used to pass encoded data to Amazon EC2 instances by way of the UserData property.",
		"scope": "source.cloudformation"
	},
	"Fn::Cidr": {
		"prefix": "cidr",
		"body": "!Cidr [ ipBlock, count, cidrBits ]",
		"description": "The intrinsic function Fn::Cidr returns an array of CIDR address blocks. The number of CIDR blocks returned is dependent on the count parameter.",
		"scope": "source.cloudformation"
	},
	"Fn::FindInMap": {
		"prefix": "find-in-map",
		"body": "!FindInMap [ MappingName, keyname, value ]",
		"description": "The intrinsic function Fn::FindInMap returns the value corresponding to keys in a two-level map that is declared in the Mappings section.",
		"scope": "source.cloudformation"
	},
	"Fn::GetAtt": {
		"prefix": "get-att",
		"body": "!GetAtt logicalNameOfResource.attributeName",
		"description": "The Fn::GetAtt intrinsic function returns the value of an attribute from a resource in the template.",
		"scope": "source.cloudformation"
	},
	"Fn::GetAZs": {
		"prefix": "get-azs",
		"body": "!GetAZs region",
		"description": "The intrinsic function Fn::GetAZs returns an array that lists Availability Zones for a specified region",
		"scope": "source.cloudformation"
	},
	"Fn::ImportValue": {
		"prefix": "import-value",
		"body": "!ImportValue sharedValueToImport",
		"description": "The intrinsic function Fn::ImportValue returns the value of an output exported by another stack.",
		"scope": "source.cloudformation"
	},
	"Fn::Join": {
		"prefix": "join",
		"body": "!Join [ delimiter, [ comma-delimited list of values ] ]",
		"description": "The intrinsic function Fn::Join appends a set of values into a single value, separated by the specified delimiter.",
		"scope": "source.cloudformation"
	},
	"Fn::Select": {
		"prefix": "select",
		"body": "!Select [ index, listOfObjects ]",
		"description": "The intrinsic function Fn::Select returns a single object from a list of objects by index.",
		"scope": "source.cloudformation"
	},
	"Fn::Split": {
		"prefix": "split",
		"body": "!Split [ delimiter, source string ]",
		"description": "To split a string into a list of string values so that you can select an element from the resulting string list, use the Fn::Split intrinsic function.",
		"scope": "source.cloudformation"
	},
	"Fn::Sub": {
		"prefix": "sub",
		"body": "!Sub String",
		"description": "The intrinsic function Fn::Sub substitutes variables in an input string with values that you specify.",
		"scope": "source.cloudformation"
	},
	"Fn::Transform": {
		"prefix": "transform",
		"body": "!Transform { 'Name' : macro name, 'Parameters' : {key : value, ... } }",
		"description": "The intrinsic function Fn::Transform specifies a macro to perform custom processing on part of a stack template.",
		"scope": "source.cloudformation"
	},
	"Ref": {
		"prefix": "ref",
		"body": "!Ref logicalName",
		"description": "The intrinsic function Ref returns the value of the specified parameter or resource.",
		"scope": "source.cloudformation"
	}
}
